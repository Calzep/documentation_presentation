/**
 * Takes a string and returns it in reverse
 *
 * @param {string} stringInput - description
 * @returns {string} description
 */

export function reverseString (stringInput){
    return stringInput.split('').reverse().join('')
}
